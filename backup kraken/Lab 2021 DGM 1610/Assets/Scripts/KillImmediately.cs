using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KillImmediately : MonoBehaviour
{
    void Start()
    {
        Destroy(gameObject);
    }
// currently being used as a bandaid to delete object im creating just to avoid errors.
}
